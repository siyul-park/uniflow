- kind: listener
  name: listener
  protocol: http
  port: '{{ .PORT }}'
  ports:
    out:
      - name: proxy
        port: in
  env:
    PORT:
      - data: '{{ .PORT }}'

- kind: http
  name: proxy
  url: https://echo.free.beeceptor.com/
