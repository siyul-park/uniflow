- kind: listener
  name: listener
  protocol: http
  port: '{{ .PORT }}'
  ports:
    out:
      - name: router
        port: in
  env:
    PORT:
      data: '{{ .PORT }}'
- kind: proxy
  name: proxy
  urls: [https://www.google.com/]
