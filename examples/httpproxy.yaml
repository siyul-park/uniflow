- kind: listener
  name: listener
  protocol: http
  port: '{{ .PORT }}'
  ports:
    out:
      - name: proxy
        port: in
  env:
    PORT:
      data: '{{ .PORT }}'

- kind: proxy
  name: proxy
  urls: [https://www.google.com/]
